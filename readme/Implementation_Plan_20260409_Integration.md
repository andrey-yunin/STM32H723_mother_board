# План реализации: Бесшовная интеграция Дирижер - Исполнители
**Дата:** 9 апреля 2026 г.
**Цель:** Приведение ПО Дирижера к стандарту «Директива 2.0» с разделением System/Device слоев.

## Этап 1: Модернизация Транспорта (CAN Packer)
- [x] **1.1. Рефакторинг `can_packer.h`:** Добавлены команды, NodeID, макросы BUILD_ID и унифицированные прототипы.
- [x] **1.2. Рефакторинг `can_packer.c`:** Реализован строгий DLC=8 и унифицированный Payload [CMD, CH, PARAM].
- [x] **1.3. Реструктуризация `Packer_ParseCanResponse`:** 
    - [x] Реализовать парсинг подтипов (DONE/DATA/LOG) с унифицированным смещением (CMD в 1-2 байтах, CH в 3-м).
    - [x] Добавить поддержку 32-битных данных в `DATA` с конвертацией Endianness (LE -> BE).
- [x] **1.4. Единый реестр NACK:** 
    - [x] Синхронизировать `can_packer.h` с кодами ошибок 0x0001-0x0007 из проектов исполнителей.

## Этап 2: Слой Маппинга и Инициализации (System vs Device)
- [x] **2.1. Рефакторинг `system_mapping.h`:** Создан слой системной логики (ID устройств для Хоста и Рецептов).
- [x] **2.2. Создание `device_mapping.h/.c`:** Реализован физический маппинг `SYS_ID` -> `(NodeID, Channel)`.
- [x] **2.3. Массовая замена `DEV_` на `SYS_`:** Код проекта (job_manager, recipe_store, param_translator) приведен к единому стандарту.
- [ ] **2.4. Сервисная инфраструктура (Full Discovery):**
    - [x] Реализовать `ServiceManager` для управления инвентаризацией.
    - [x] Добавить в `JobManager_Run` обработку ответов на `0xF001` (Info) и `0xF004` (UID).
    - [x] Реализовать процедуру `Mapping_SetNodeID` для горячей замены плат.
    - [x] Внедрить проверку "Списка присутствия" в рецепт `INIT`.

## Этап 3: Рефакторинг Диспетчера (JobManager)
- [x] 3.1. Интеграция `DeviceMapping`: Внедрен вызов трансляции адреса в `JobManager_ExecuteStep`.
- [x] 3.2. Обновление API: Все вызовы упаковщика переведены на новый стандарт (убран `job_id`).
- [x] 3.3. Унификация ответов Хосту (DATA/DONE/LOG проброс с Endianness conversion).

## Этап 4: Валидация (Первичная)
- [x] **4.1. Контрольная точка "Сборка":** Проект успешно компилируется без предупреждений.
- [x] **4.2. Тест "Discovery":** Проверка обнаружения плат по UID и назначения NodeID.
- [x] **4.3. Интеграционный тест E2E:** Хост (`CM> 0x2000`) -> System Logic -> Physical CAN -> Хост.
- [ ] **4.4. Тест "Warm Finger":** Проверка получения температуры и корректности Big-endian упаковки для Хоста.

## Этап 5: Системная Валидация (Advanced Testing Strategy)
- [x] **5.1. Тестовая фаза A: Регрессия (Python Scripts)**
    - [ ] Запуск `test_combined_commands.py` для проверки стабильности USB-канала.
    - [x] Валидация бинарных ответов `CM>` после рефакторинга на `test_main_processes.py`.
- [x] **5.2. Тестовая фаза B: Эмуляция линка (CAN Adapter)**
    - [x] Эмуляция ответов Исполнителя через сторонний адаптер CANable/SocketCAN.
    - [x] Подтвержден физический CAN TX/RX Дирижера после исправления подключения пинов МК.
    - [x] `App_user/can_test.py --can-only-responder` эмулирует Motion `0x20` и Fluidics `0x30`.
    - [x] Полный `test_main_processes.py` прошел через Host USB -> Conductor -> CAN fake executors -> Host.
    - [ ] Проверка корректности Big-Endian трансляции в пакетах `DATA`.
    - [ ] Верификация проброса текстовых `DEBUG` логов от реального Executor.
- [ ] **5.3. Тестовая фаза C: Стресс и Ошибки**
    - [ ] Проверка поведения при обрыве связи (Timeout) и потере питания исполнителя.
    - [ ] Тестирование коллизий при одновременном ответе нескольких плат.

## Этап 6: Доведение до промышленного стандарта DDS-240
- [ ] **6.1. Сверка Conductor с `dds240_global_config.h`:**
    - [ ] NodeID, DeviceType, service-команды `0xF001..0xF007`, NACK registry, strict DLC policy.
    - [ ] Убрать расхождения между локальными `can_packer.h` и общим стандартом экосистемы.
- [ ] **6.2. Полный Discovery/Inventory:**
    - [ ] Обновление инвентаря по DATA-ответу `0xF001` с `device_type`, FW и UID.
    - [ ] Поддержка `GET_UID (0xF004)`, `GET_STATUS (0xF007)`, `REBOOT/COMMIT/FACTORY_RESET` recovery policy.
    - [ ] Проверка baseline/delta для `GET_STATUS` метрик Executor-плат.
- [ ] **6.3. Реальные исполнители:**
    - [ ] Подключить реальную Fluidics `0x30` и прогнать `GET_DEVICE_INFO`, `GET_STATUS`, `PUMP_START/STOP`, `RUN_DURATION`.
    - [ ] Подключить Motion `0x20` и проверить `HOME/ROTATE/START_CONTINUOUS/STOP` на реальном железе.
    - [ ] Подключить Thermo `0x40` и закрыть `Warm Finger` / Big-Endian DATA path.
- [ ] **6.4. Тестовая инфраструктура:**
    - [ ] Привести ожидания `test_main_processes.py` к новому формату логов `Phys:<node>:<channel>`.
    - [ ] Добавить режимы выборочного запуска: USB status-only, INIT-only, recipe-only.
    - [ ] Зафиксировать CANable E2E сценарий как обязательный smoke-test перед реальными исполнителями.

---
**Итог сессии 13.04.2026:** API Хост-Дирижер и Дирижер-Исполнитель полностью синхронизированы. Код валидирован, варнинги устранены. Система готова к завтрашним тестам Phase B.

**Итог сессии 22.04.2026:** Phase B закрыта на стенде CANable без реальных исполнителей. Подтверждены физический CAN TX/RX Дирижера, парсинг high-level Host-команд по USB, трансляция рецептов в CAN-команды Motion/Fluidics и завершение транзакций по fake `ACK/DONE`. Следующий этап - доведение Conductor до общего стандарта DDS-240 и регрессия с реальными Executor-платами.
